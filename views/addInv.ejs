<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Location Registration</title>
    <link rel="stylesheet" type="text/css" href="css/register.css">
    <link href="../public/output.css" rel="stylesheet"></link>
    <div class="logo">
        <img src="???"><a href="/dashboard">Home</a>
    </div>
</head>
<body>
    <div class = "AddInvPage">
        <% if (allItems) { %>
        <h2>Add an Item to Inventory</h2><br>
        <form action="/dashboard/api/addInv" method="POST" id="InvForm">
            <label for="item">Item Name:</label>
            <select name="itemID" id="itemID" required>
                <option value="">--Please Select Item to add to your Inventory--</option>
                <% for (const i of allItems) { %>
                <option value="<%=i.itemID%>"><%=i.itemName%> (<%=i.itemBrand%>)</option>
            <% } %>
            </select><br><br>
            <label for="quantity">Item Quantity:</label>
            <input type="number" id="quantity" name="quantity" required><br><br>

            <button type="submit">Submit</button>
        </form>
        <% } else { %>
            <h2>No Items Found.</h2>
            <div class="createItemCard">
                <h3><a href="/dashboard/createItem">Create a New Item</a></h3>
            </div>
        <% } %>
        <br><br>
    </div>
</body>
</html>